{% for servers in nginx_servers %}
server {

        server_name {{ servers.server_name }};

        location / {
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_pass {{ servers.proxy_pass }};
        }
}
{% endfor %}
